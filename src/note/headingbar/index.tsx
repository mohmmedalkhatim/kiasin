import { Editor } from '@tiptap/react';

function HeadingBar ({ editor }: { editor: Editor }) {

  return <nav className='heading_bar w-[17rem] m_border border-r'></nav>;
}
export default HeadingBar;
