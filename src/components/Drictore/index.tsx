


function Drictore({ name,props }: { name: string,props:any }) {
    switch (name) {
        case "list": {
            return  
        }
        default: {
            return <div>
                couldn't find the component
            </div>
        }

    }
}
export default Drictore;